/** In the name of Allah, the Most Gracious and the Most Merciful **/

/**
    Code of :

    Author : Pavel
    Member of "Happy Coding".

    Southeast University.

                                **/

#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long int t,h,s;
    cin>>t;
    while(t--)
    {
        cin>>h>>s;
    double x;
    x=((s*s)-(h*h))/(2.0*h);
    cout<<setprecision(8)<<fixed<<x<<endl;
    }

    return 0;
}
